<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inicio - Biblioteca Digital</title>
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/css/style.css">
    
    <style>
        .hero-section {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 4rem 0;
            margin-bottom: 3rem;
        }
        
        .section-title {
            font-size: 2.5rem;
            font-weight: bold;
            margin-bottom: 1rem;
            color: #2c3e50;
        }
        
        .section-subtitle {
            color: #7f8c8d;
            margin-bottom: 2rem;
        }
        
        .book-card {
            transition: all 0.3s ease;
            height: 100%;
        }
        
        .book-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(0,0,0,0.15);
        }
        
        .book-cover {
            height: 200px;
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 3rem;
            position: relative;
            overflow: hidden;
        }
        
        .book-cover img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        
        .book-rating {
            color: #ffc107;
            font-size: 0.9rem;
        }
        
        .category-badge {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 0.25rem 0.75rem;
            border-radius: 20px;
            font-size: 0.8rem;
        }
        
        .stats-card {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border-radius: 15px;
            padding: 2rem;
            text-align: center;
            transition: all 0.3s ease;
        }
        
        .stats-card:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 35px rgba(102, 126, 234, 0.3);
        }
        
        .search-box {
            position: relative;
            max-width: 500px;
            margin: 0 auto;
        }
        
        .search-box input {
            border-radius: 50px;
            padding-right: 50px;
            border: 2px solid #e9ecef;
            transition: all 0.3s ease;
        }
        
        .search-box input:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 0.2rem rgba(102, 126, 234, 0.25);
        }
        
        .search-box button {
            position: absolute;
            right: 5px;
            top: 50%;
            transform: translateY(-50%);
            border-radius: 50%;
            width: 40px;
            height: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .category-pills {
            display: flex;
            flex-wrap: wrap;
            gap: 0.5rem;
            justify-content: center;
            margin: 2rem 0;
        }
        
        .category-pill {
            background: white;
            color: #667eea;
            border: 2px solid #667eea;
            padding: 0.5rem 1.5rem;
            border-radius: 25px;
            text-decoration: none;
            transition: all 0.3s ease;
            font-weight: 500;
        }
        
        .category-pill:hover {
            background: #667eea;
            color: white;
            transform: translateY(-2px);
        }
        
        @media (max-width: 768px) {
            .hero-section {
                padding: 2rem 0;
            }
            
            .section-title {
                font-size: 1.8rem;
            }
            
            .stats-card {
                margin-bottom: 1rem;
            }
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand d-flex align-items-center" href="/">
                <i class="bi bi-book-half me-2"></i>
                <strong>Biblioteca Digital</strong>
            </a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link active" href="/">
                            <i class="bi bi-house-door me-1"></i>Inicio
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/catalog">
                            <i class="bi bi-grid me-1"></i>Catálogo
                        </a>
                    </li>
                    <% if (user) { %>
                        <% if (user.rol === 'admin') { %>
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                                    <i class="bi bi-gear me-1"></i>Administración
                                </a>
                                <ul class="dropdown-menu">
                                    <li><a class="dropdown-item" href="/admin/dashboard">
                                        <i class="bi bi-speedometer2 me-2"></i>Dashboard
                                    </a></li>
                                    <li><a class="dropdown-item" href="/admin/books">
                                        <i class="bi bi-book me-2"></i>Libros
                                    </a></li>
                                    <li><a class="dropdown-item" href="/admin/users">
                                        <i class="bi bi-people me-2"></i>Usuarios
                                    </a></li>
                                </ul>
                            </li>
                        <% } %>
                        <li class="nav-item">
                            <a class="nav-link" href="/favorites">
                                <i class="bi bi-heart me-1"></i>Favoritos
                            </a>
                        </li>
                    <% } %>
                </ul>
                
                <ul class="navbar-nav">
                    <% if (user) { %>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                                <i class="bi bi-person-circle me-1"></i><%= user.nombre %> 
                                <% if (user.rol === 'admin') { %>
                                    <span class="badge bg-warning text-dark">Admin</span>
                                <% } %>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end">
                                <li><a class="dropdown-item" href="#">
                                    <i class="bi bi-key me-2"></i>Cambiar Contraseña
                                </a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item text-danger" href="/auth/logout">
                                    <i class="bi bi-box-arrow-right me-2"></i>Cerrar Sesión
                                </a></li>
                            </ul>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/auth/login">
                                <i class="bi bi-box-arrow-in-right me-1"></i>Iniciar Sesión
                            </a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>
    
    <!-- Hero Section -->
    <section class="hero-section">
        <div class="container text-center">
            <h1 class="display-4 fw-bold mb-4">Biblioteca Digital</h1>
            <p class="lead mb-4">Explora miles de libros digitales en un solo lugar</p>
            
            <!-- Search Box -->
            <form action="/catalog" method="GET" class="search-box">
                <input type="text" name="busqueda" class="form-control form-control-lg" placeholder="Buscar libros, autores...">
                <button type="submit" class="btn btn-warning">
                    <i class="bi bi-search"></i>
                </button>
            </form>
        </div>
    </section>
    
    <!-- Main Content -->
    <div class="container">
        <!-- Categories -->
        <section class="mb-5">
            <h2 class="section-title text-center">Explorar por Categorías</h2>
            <div class="category-pills">
                <% categorias.forEach(categoria => { %>
                    <a href="/catalog?categoria_id=<%= categoria.id %>" class="category-pill">
                        <i class="bi bi-tag me-1"></i><%= categoria.nombre %>
                    </a>
                <% }) %>
            </div>
        </section>
        
        <!-- Popular Books -->
        <section class="mb-5">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <div>
                    <h2 class="section-title mb-0">Libros Populares</h2>
                    <p class="section-subtitle">Los más descargados y leídos</p>
                </div>
                <a href="/catalog?ordenar=vistas_count&direccion=DESC" class="btn btn-outline-primary">
                    <i class="bi bi-arrow-right me-1"></i>Ver Todos
                </a>
            </div>
            
            <div class="row g-4">
                <% librosPopulares.forEach(libro => { %>
                    <div class="col-md-4 col-lg-3">
                        <div class="card book-card h-100">
                            <div class="book-cover">
                                <% if (libro.portada_url) { %>
                                    <img src="<%= libro.portada_url %>" alt="<%= libro.titulo %>">
                                <% } else { %>
                                    <i class="bi bi-book"></i>
                                <% } %>
                            </div>
                            <div class="card-body">
                                <h6 class="card-title text-truncate" title="<%= libro.titulo %>">
                                    <%= libro.titulo %>
                                </h6>
                                <p class="card-text small text-muted mb-2">
                                    <i class="bi bi-person me-1"></i><%= libro.autor %>
                                </p>
                                <div class="d-flex justify-content-between align-items-center">
                                    <span class="book-rating">
                                        <i class="bi bi-star-fill"></i> <%= libro.promedio_calificacion || 'N/A' %>
                                    </span>
                                    <small class="text-muted">
                                        <i class="bi bi-eye me-1"></i><%= libro.vistas_count || 0 %>
                                    </small>
                                </div>
                            </div>
                            <div class="card-footer bg-transparent border-0">
                                <a href="/library/books/<%= libro.id %>" class="btn btn-primary btn-sm w-100">
                                    <i class="bi bi-book me-1"></i>Ver Detalles
                                </a>
                            </div>
                        </div>
                    </div>
                <% }) %>
            </div>
        </section>
        
        <!-- Best Rated Books -->
        <section class="mb-5">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <div>
                    <h2 class="section-title mb-0">Mejor Calificados</h2>
                    <p class="section-subtitle">Libros con mejores reseñas</p>
                </div>
                <a href="/catalog?ordenar=promedio_calificacion&direccion=DESC" class="btn btn-outline-success">
                    <i class="bi bi-arrow-right me-1"></i>Ver Todos
                </a>
            </div>
            
            <div class="row g-4">
                <% librosMejorCalificados.forEach(libro => { %>
                    <div class="col-md-4 col-lg-3">
                        <div class="card book-card h-100">
                            <div class="book-cover">
                                <% if (libro.portada_url) { %>
                                    <img src="<%= libro.portada_url %>" alt="<%= libro.titulo %>">
                                <% } else { %>
                                    <i class="bi bi-book"></i>
                                <% } %>
                            </div>
                            <div class="card-body">
                                <h6 class="card-title text-truncate" title="<%= libro.titulo %>">
                                    <%= libro.titulo %>
                                </h6>
                                <p class="card-text small text-muted mb-2">
                                    <i class="bi bi-person me-1"></i><%= libro.autor %>
                                </p>
                                <div class="d-flex justify-content-between align-items-center">
                                    <span class="book-rating">
                                        <i class="bi bi-star-fill"></i> <%= libro.promedio_calificacion || 'N/A' %>
                                    </span>
                                    <small class="text-muted">
                                        <i class="bi bi-chat-dots me-1"></i><%= libro.total_comentarios || 0 %>
                                    </small>
                                </div>
                            </div>
                            <div class="card-footer bg-transparent border-0">
                                <a href="/library/books/<%= libro.id %>" class="btn btn-success btn-sm w-100">
                                    <i class="bi bi-book me-1"></i>Ver Detalles
                                </a>
                            </div>
                        </div>
                    </div>
                <% }) %>
            </div>
        </section>
        
        <!-- Quick Stats -->
        <% if (user && user.rol === 'admin') { %>
            <section class="mb-5">
                <h2 class="section-title text-center">Estadísticas Rápidas</h2>
                <div class="row g-4">
                    <div class="col-md-3">
                        <div class="stats-card">
                            <i class="bi bi-book display-4 mb-3"></i>
                            <h3><%= librosPopulares.length %></h3>
                            <p>Libros Populares</p>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="stats-card">
                            <i class="bi bi-tags display-4 mb-3"></i>
                            <h3><%= categorias.length %></h3>
                            <p>Categorías</p>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="stats-card">
                            <i class="bi bi-star display-4 mb-3"></i>
                            <h3><%= librosMejorCalificados.length %></h3>
                            <p>Libros Calificados</p>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="stats-card">
                            <i class="bi bi-heart display-4 mb-3"></i>
                            <h3><%= librosMasFavoritados.length %></h3>
                            <p>Favoritos</p>
                        </div>
                    </div>
                </div>
            </section>
        <% } %>
    </div>
    
    <!-- Footer -->
    <footer class="bg-dark text-light py-4 mt-5">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <h5><i class="bi bi-book-half me-2"></i>Biblioteca Digital</h5>
                    <p class="text-muted">Plataforma de gestión de biblioteca digital</p>
                </div>
                <div class="col-md-6 text-md-end">
                    <p class="text-muted mb-0">© 2024 Biblioteca Digital</p>
                </div>
            </div>
        </div>
    </footer>
    
    <!-- Bootstrap 5 JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Custom JavaScript -->
    <script src="/js/main.js"></script>
</body>
</html>