<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
        <!-- Logo/Brand -->
        <a class="navbar-brand d-flex align-items-center" href="/">
            <i class="bi bi-book-half me-2"></i>
            <strong>Biblioteca Digital</strong>
        </a>
        
        <!-- Mobile toggle button -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <!-- Navigation menu -->
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <!-- Public navigation -->
                <li class="nav-item">
                    <a class="nav-link <%= (typeof activeNav !== 'undefined' && activeNav === 'home') ? 'active' : '' %>" href="/">
                        <i class="bi bi-house-door me-1"></i>Inicio
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= (typeof activeNav !== 'undefined' && activeNav === 'catalog') ? 'active' : '' %>" href="/catalog">
                        <i class="bi bi-grid me-1"></i>Catálogo
                    </a>
                </li>
                
                <!-- Authenticated user navigation -->
                <% if (typeof user !== 'undefined' && user) { %>
                    <% if (user.rol === 'admin') { %>
                        <!-- Admin navigation -->
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle <%= (typeof activeNav !== 'undefined' && activeNav === 'admin') ? 'active' : '' %>" href="#" role="button" data-bs-toggle="dropdown">
                                <i class="bi bi-gear me-1"></i>Administración
                            </a>
                            <ul class="dropdown-menu">
                                <li><a class="dropdown-item" href="/admin/dashboard">
                                    <i class="bi bi-speedometer2 me-2"></i>Dashboard
                                </a></li>
                                <li><a class="dropdown-item" href="/admin/users">
                                    <i class="bi bi-people me-2"></i>Usuarios
                                </a></li>
                                <li><a class="dropdown-item" href="/admin/books">
                                    <i class="bi bi-book me-2"></i>Libros
                                </a></li>
                                <li><a class="dropdown-item" href="/admin/matriculas">
                                    <i class="bi bi-card-checklist me-2"></i>Matrículas
                                </a></li>
                                <li><a class="dropdown-item" href="/admin/categories">
                                    <i class="bi bi-tags me-2"></i>Categorías
                                </a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="/admin/comments">
                                    <i class="bi bi-chat-dots me-2"></i>Comentarios
                                </a></li>
                            </ul>
                        </li>
                    <% } %>
                    
                    <!-- User-specific navigation -->
                    <li class="nav-item">
                        <a class="nav-link <%= (typeof activeNav !== 'undefined' && activeNav === 'favorites') ? 'active' : '' %>" href="/favorites">
                            <i class="bi bi-heart me-1"></i>Mis Favoritos
                        </a>
                    </li>
                <% } %>
            </ul>
            
            <!-- Right side navigation -->
            <ul class="navbar-nav">
                <% if (typeof user !== 'undefined' && user) { %>
                    <!-- Authenticated user -->
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button" data-bs-toggle="dropdown">
                            <div class="bg-light rounded-circle p-1 me-2">
                                <i class="bi bi-person-fill text-primary"></i>
                            </div>
                            <%= user.nombre %> <%= user.apellido %>
                            <% if (user.rol === 'admin') { %>
                                <span class="badge bg-warning text-dark ms-2">Admin</span>
                            <% } else if (typeof matriculaActiva !== 'undefined' && matriculaActiva) { %>
                                <span class="badge bg-success ms-2">Matriculado</span>
                            <% } else { %>
                                <span class="badge bg-secondary ms-2">No matriculado</span>
                            <% } %>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <% if (user.rol !== 'admin') { %>
                                <li><h6 class="dropdown-header">Información de Usuario</h6></li>
                                <li><span class="dropdown-item-text">
                                    <small class="text-muted">DNI: <%= user.dni %></small><br>
                                    <small class="text-muted">Email: <%= user.email %></small>
                                </span></li>
                                <li><hr class="dropdown-divider"></li>
                            <% } %>
                            <li><a class="dropdown-item" href="#" onclick="showChangePasswordModal()">
                                <i class="bi bi-key me-2"></i>Cambiar Contraseña
                            </a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item text-danger" href="/auth/logout">
                                <i class="bi bi-box-arrow-right me-2"></i>Cerrar Sesión
                            </a></li>
                        </ul>
                    </li>
                <% } else { %>
                    <!-- Not authenticated -->
                    <li class="nav-item">
                        <a class="nav-link" href="/auth/login">
                            <i class="bi bi-box-arrow-in-right me-1"></i>Iniciar Sesión
                        </a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>

<!-- Change Password Modal (only for authenticated users) -->
<% if (typeof user !== 'undefined' && user) { %>
<div class="modal fade" id="changePasswordModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Cambiar Contraseña</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <form id="changePasswordForm">
                    <div class="mb-3">
                        <label for="currentPassword" class="form-label">Contraseña Actual</label>
                        <input type="password" class="form-control" id="currentPassword" required>
                    </div>
                    <div class="mb-3">
                        <label for="newPassword" class="form-label">Nueva Contraseña</label>
                        <input type="password" class="form-control" id="newPassword" minlength="6" required>
                        <div class="form-text">Mínimo 6 caracteres</div>
                    </div>
                    <div class="mb-3">
                        <label for="confirmPassword" class="form-label">Confirmar Nueva Contraseña</label>
                        <input type="password" class="form-control" id="confirmPassword" minlength="6" required>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                <button type="button" class="btn btn-primary" onclick="changePassword()">Cambiar Contraseña</button>
            </div>
        </div>
    </div>
</div>
<% } %>